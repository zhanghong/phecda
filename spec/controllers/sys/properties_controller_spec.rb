require 'spec_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to specify the controller code that
# was generated by Rails when you ran the scaffold generator.
#
# It assumes that the implementation code is generated by the rails scaffold
# generator.  If you are using any extension libraries to generate different
# controller code, this generated spec may or may not pass.
#
# It only uses APIs available in rails and/or rspec-rails.  There are a number
# of tools you can use to make these specs even more expressive, but we're
# sticking to rails and rspec-rails APIs to keep things simple and stable.
#
# Compared to earlier versions of this generator, there is very limited use of
# stubs and message expectations in this spec.  Stubs are only used when there
# is no simpler way to get a handle on the object needed for the example.
# Message expectations are only used when there is no simpler way to specify
# that an instance is receiving a specific message.

describe Sys::PropertiesController do

  # This should return the minimal set of attributes required to create a valid
  # Sys::Property. As you add validations to Sys::Property, be sure to
  # adjust the attributes here as well.
  let(:valid_attributes) { { "name" => "MyString" } }

  # This should return the minimal set of values that should be in the session
  # in order to pass any filters (e.g. authentication) defined in
  # Sys::PropertiesController. Be sure to keep this updated too.
  let(:valid_session) { {} }

  describe "GET index" do
    it "assigns all sys_properties as @sys_properties" do
      property = Sys::Property.create! valid_attributes
      get :index, {}, valid_session
      assigns(:sys_properties).should eq([property])
    end
  end

  describe "GET show" do
    it "assigns the requested sys_property as @sys_property" do
      property = Sys::Property.create! valid_attributes
      get :show, {:id => property.to_param}, valid_session
      assigns(:sys_property).should eq(property)
    end
  end

  describe "GET new" do
    it "assigns a new sys_property as @sys_property" do
      get :new, {}, valid_session
      assigns(:sys_property).should be_a_new(Sys::Property)
    end
  end

  describe "GET edit" do
    it "assigns the requested sys_property as @sys_property" do
      property = Sys::Property.create! valid_attributes
      get :edit, {:id => property.to_param}, valid_session
      assigns(:sys_property).should eq(property)
    end
  end

  describe "POST create" do
    describe "with valid params" do
      it "creates a new Sys::Property" do
        expect {
          post :create, {:sys_property => valid_attributes}, valid_session
        }.to change(Sys::Property, :count).by(1)
      end

      it "assigns a newly created sys_property as @sys_property" do
        post :create, {:sys_property => valid_attributes}, valid_session
        assigns(:sys_property).should be_a(Sys::Property)
        assigns(:sys_property).should be_persisted
      end

      it "redirects to the created sys_property" do
        post :create, {:sys_property => valid_attributes}, valid_session
        response.should redirect_to(Sys::Property.last)
      end
    end

    describe "with invalid params" do
      it "assigns a newly created but unsaved sys_property as @sys_property" do
        # Trigger the behavior that occurs when invalid params are submitted
        Sys::Property.any_instance.stub(:save).and_return(false)
        post :create, {:sys_property => { "name" => "invalid value" }}, valid_session
        assigns(:sys_property).should be_a_new(Sys::Property)
      end

      it "re-renders the 'new' template" do
        # Trigger the behavior that occurs when invalid params are submitted
        Sys::Property.any_instance.stub(:save).and_return(false)
        post :create, {:sys_property => { "name" => "invalid value" }}, valid_session
        response.should render_template("new")
      end
    end
  end

  describe "PUT update" do
    describe "with valid params" do
      it "updates the requested sys_property" do
        property = Sys::Property.create! valid_attributes
        # Assuming there are no other sys_properties in the database, this
        # specifies that the Sys::Property created on the previous line
        # receives the :update_attributes message with whatever params are
        # submitted in the request.
        Sys::Property.any_instance.should_receive(:update).with({ "name" => "MyString" })
        put :update, {:id => property.to_param, :sys_property => { "name" => "MyString" }}, valid_session
      end

      it "assigns the requested sys_property as @sys_property" do
        property = Sys::Property.create! valid_attributes
        put :update, {:id => property.to_param, :sys_property => valid_attributes}, valid_session
        assigns(:sys_property).should eq(property)
      end

      it "redirects to the sys_property" do
        property = Sys::Property.create! valid_attributes
        put :update, {:id => property.to_param, :sys_property => valid_attributes}, valid_session
        response.should redirect_to(property)
      end
    end

    describe "with invalid params" do
      it "assigns the sys_property as @sys_property" do
        property = Sys::Property.create! valid_attributes
        # Trigger the behavior that occurs when invalid params are submitted
        Sys::Property.any_instance.stub(:save).and_return(false)
        put :update, {:id => property.to_param, :sys_property => { "name" => "invalid value" }}, valid_session
        assigns(:sys_property).should eq(property)
      end

      it "re-renders the 'edit' template" do
        property = Sys::Property.create! valid_attributes
        # Trigger the behavior that occurs when invalid params are submitted
        Sys::Property.any_instance.stub(:save).and_return(false)
        put :update, {:id => property.to_param, :sys_property => { "name" => "invalid value" }}, valid_session
        response.should render_template("edit")
      end
    end
  end

  describe "DELETE destroy" do
    it "destroys the requested sys_property" do
      property = Sys::Property.create! valid_attributes
      expect {
        delete :destroy, {:id => property.to_param}, valid_session
      }.to change(Sys::Property, :count).by(-1)
    end

    it "redirects to the sys_properties list" do
      property = Sys::Property.create! valid_attributes
      delete :destroy, {:id => property.to_param}, valid_session
      response.should redirect_to(sys_properties_url)
    end
  end

end
