<h1>Listing core_user_roles</h1>

<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Role</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @core_user_roles.each do |core_user_role| %>
      <tr>
        <td><%= core_user_role.user_id %></td>
        <td><%= core_user_role.role_id %></td>
        <td><%= link_to 'Show', core_user_role %></td>
        <td><%= link_to 'Edit', edit_core_user_role_path(core_user_role) %></td>
        <td><%= link_to 'Destroy', core_user_role, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New User role', new_core_user_role_path %>
